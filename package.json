{
  "name": "little-world",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node scripts/dev.js",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0",
    "test": "node --test src/*.test.js socket-server/*.test.js",
    "dev:client": "vite",
    "dev:server": "node --watch socket-server/index.js"
  },
  "dependencies": {
    "three": "^0.180.0"
  },
  "devDependencies": {
    "vite": "^7.1.0"
  },
  "workspaces": [
    "socket-server"
  ]
}
